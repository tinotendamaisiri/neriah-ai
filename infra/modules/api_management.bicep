// Stub — API Management provisioning takes 30+ minutes.
// Deploy this separately after core services are live.
param location string
param serviceName string
output note string = 'Deploy API Management separately after core services are confirmed live.'
