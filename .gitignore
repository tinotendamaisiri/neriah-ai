__pycache__/
*.pyc
.env
.env.*
.env.local
backend/.env
neriah-website/.env.local
.venv/
venv/
*.egg-info/
.DS_Store
credentials.json
*.pem
*.key

# React Native / Expo
node_modules/
.expo/
.vercel

# AI model files (downloaded at runtime, not bundled)
*.task
app/mobile/assets/models/

# EAS build artifacts pulled down for local install (not committed)
app/mobile/build-*.apk

# Editor / OS / one-off backup files
*.bak
*.swp
*.swo

# Stray root-level lockfile from accidental npm runs
/package-lock.json
